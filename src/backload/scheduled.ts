import type { Env } from '../env';

export const scheduledHandler: ExportedHandlerScheduledHandler<Env> = async (
  _controller,
  _env,
  _ctx,
) => {
  // Placeholder — scheduled backload handler implementation TBD
};
